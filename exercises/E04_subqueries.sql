-- ============================================================================
--  E04_subqueries  —  Фаза 2
--  Тема: Підзапити: скалярні, IN/EXISTS, корельовані, у FROM
--  БД: shop   (SET search_path = shop, public;)
--
--  Цей файл — заготовка. Завдання сюди додає Claude Code, коли ти дійдеш до
--  відповідної фази: попроси "дай вправи E04_subqueries" / "дай вправи з <тема>".
--  Розв'язки складай поряд у solutions/E04_subqueries.sql і проси рев'ю.
-- ============================================================================
SET search_path = shop, public;

-- (завдання буде додано на відповідній фазі)
