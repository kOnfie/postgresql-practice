-- ============================================================================
--  E14_pagination  —  Фаза 8
--  Тема: OFFSET vs keyset пагінація, заміри на глибині
--  БД: shop   (SET search_path = shop, public;)
--
--  Цей файл — заготовка. Завдання сюди додає Claude Code, коли ти дійдеш до
--  відповідної фази: попроси "дай вправи E14_pagination" / "дай вправи з <тема>".
--  Розв'язки складай поряд у solutions/E14_pagination.sql і проси рев'ю.
-- ============================================================================
SET search_path = shop, public;

-- (завдання буде додано на відповідній фазі)
