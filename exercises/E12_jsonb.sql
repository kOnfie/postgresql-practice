-- ============================================================================
--  E12_jsonb  —  Фаза 8
--  Тема: JSONB: оператори, шляхи, GIN vs expr-btree, розгортання
--  БД: shop   (SET search_path = shop, public;)
--
--  Цей файл — заготовка. Завдання сюди додає Claude Code, коли ти дійдеш до
--  відповідної фази: попроси "дай вправи E12_jsonb" / "дай вправи з <тема>".
--  Розв'язки складай поряд у solutions/E12_jsonb.sql і проси рев'ю.
-- ============================================================================
SET search_path = shop, public;

-- (завдання буде додано на відповідній фазі)
