-- ============================================================================
--  E02. JOIN — від INNER до LATERAL
--  БД: shop   (search_path = shop, public)
--  Як робити: пиши свій запит під кожним пунктом, виконуй, звіряй з
--             solutions/E02_joins.sql ЛИШЕ після власної спроби.
--  Для 2-3 задач додатково зроби EXPLAIN (ANALYZE, BUFFERS) і поклади
--  спостереження в notes/02-joins.md.
-- ============================================================================
SET search_path = shop, public;

-- 1. Для кожного замовлення виведи id замовлення, email клієнта і total_cents.
--    (INNER JOIN orders + customers)


-- 2. Виведи всіх клієнтів і кількість їхніх замовлень, включно з тими, у кого
--    замовлень 0. (LEFT JOIN + GROUP BY; подумай, count(*) чи count(o.id))


-- 3. Замовлення разом із сумою за позиціями (order_items): id, status,
--    sum(qty*unit_price_cents). Переконайся, що сума НЕ роздувається.


-- 4. Знайди замовлення, у яких orders.total_cents НЕ дорівнює сумі за
--    order_items (розсинхрон денормалізованого поля). Виведи різницю.


-- 5. Для кожного товару: title, назва категорії, ім'я продавця.
--    (3 INNER JOIN)


-- 6. Товари, які жодного разу не купували. Двома способами:
--    (а) LEFT JOIN order_items ... WHERE oi.product_id IS NULL
--    (б) NOT EXISTS
--    Порівняй плани.


-- 7. Клієнти, які робили замовлення, але жодного разу не залишали відгук.
--    (антиджойн customers проти product_reviews)


-- 8. Для кожної країни: кількість клієнтів і кількість доставлених замовлень
--    (status='delivered'). Країни без замовлень теж покажи (0).


-- 9. LEFT JOIN payments: для кожного замовлення суму 'captured'-платежів.
--    Зроби це фільтром у ON (щоб замовлення без captured-платежу лишились з NULL).
--    Потім переклади фільтр у WHERE і поясни в коментарі, що змінилось.


-- 10. FULL OUTER JOIN: звір product_id, що зустрічаються в order_items, проти
--     product_id у product_reviews. Виведи 3 групи: тільки продавались,
--     тільки відгуки, і там і там (через GROUPING/CASE або 3 запити).


-- 11. CROSS JOIN: для кожної пари (категорія 1-го рівня, місяць 2025 року)
--     виведи виручку. Місяці згенеруй через generate_series. Місяці без
--     продажів мають показати 0 (тому CROSS JOIN + LEFT JOIN фактів).


-- 12. LATERAL: для кожного клієнта виведи його 3 найдорожчі замовлення
--     (customer email, order id, total_cents), відсортовані спаданням суми.
--     Клієнтів без замовлень можна пропустити (CROSS JOIN LATERAL) або
--     показати (LEFT JOIN LATERAL ... ON true).
